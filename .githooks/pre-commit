#!/bin/sh
cd "$(git rev-parse --show-toplevel)" || exit 1

if ! command -v bun >/dev/null 2>&1; then
  echo "Iron law: Bun is required. npm is forbidden."
  echo "Install Bun: https://bun.sh"
  exit 1
fi

exec bun scripts/check-no-npm.ts
